# Live Leaderboard

A single-file live leaderboard that reads scores from a Google Sheet and refreshes every 8 seconds.

Open `index.html` directly, or serve it over HTTPS (GitHub Pages) for the most reliable data path.
